---
schema_version: '1'
kind: source-summary
title: ssml builder
page_id: src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4
status: active
review_state: machine-generated
source_refs:
- src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4
generated_by_run_ids:
- run-src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4-20260430T200753740010Z
last_generated_at: '2026-04-30T20:07:53+00:00'
last_reviewed_at: null
confidence: 1.0
related_pages: []
tags:
- source-summary
- py
provenance_links:
- source_id: src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4
  page_id: null
  run_id: null
  path_ref: state/manifests/sources/src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4.json
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: run-src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4-20260430T200753740010Z
  path_ref: null
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: null
  path_ref: synthbanshee/tts/ssml_builder.py
  role: input
  note: null
contradictions: []
---

# ssml builder

## Source

- Source ID: `src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4`
- Source type: `py`
- Registered path: `synthbanshee/tts/ssml_builder.py`
- Source file: `synthbanshee/tts/ssml_builder.py`

## Summary

This page records deterministic ingestion output for source `src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4`, a `py` file registered from `synthbanshee/tts/ssml_builder.py`.

## Key Facts

- Source ID: `src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4`
- Source type: `py`
- Checksum: `3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4`
- Source ref: `synthbanshee/tts/ssml_builder.py`
- Added at: `2026-04-30T20:07:22+00:00`
- Ingested at: `2026-04-30T20:07:53+00:00`

## Extract

```text
"""SSML document builder for he-IL TTS voices (Azure and Google).

Generates SSML 1.1 documents with:
  - <voice> elements for each speaker
  - <mstts:express-as> style tags (Azure only — omitted when
    ``supports_style_tags=False``)
  - <prosody> elements for rate, pitch, and volume control
  - Nested per-phrase <prosody> + <break> elements (M2b)

Azure SSML reference:
https://learn.microsoft.com/en-us/azure/ai-services/speech-service/speech-synthesis-markup-voice
"""

from __future__ import annotations

import xml.etree.ElementTree as ET
from dataclasses import dataclass, field

from synthbanshee.tts.ssml_types import PhraseProsody

_AZURE_XMLNS = "http://www.w3.org/2001/10/synthesis"
_MSTTS_XMLNS = "http://www.w3.org/2001/mstts"
_SPEAK_LANG = "he-IL"


@dataclass
class UtteranceSpec:
    """Specifies one TTS utterance to be included in an SSML document."""

    text: str
    voice_id: str  # e.g. "he-IL-AvriNeural"
    style: str = "General"  # Azure mstts:express-as style
    rate_multiplier: float = 1.0  # speaking rate relative to voice default
    pitch_delta_st: float = 0.0  # pitch offset in semitones
    volume_delta_db: float = 0.0  # volume offset in dB
    phrase_prosody: list[PhraseProsody] = field(default_factory=list)


def _semitones_to_percent(st: float) -> str:
    """Convert a semitone shift to the Azure pitch % format (e.g. '+5%' / '-10%')."""
    # Approximation: 1 semitone ≈ 5.946% pitch change
    pct = round(st * 5.946)
    return f"+{pct}%" if pct >= 0 else f"{pct}%"


def _rate_to_string(rate: float) -> str:
    """Format a rate multiplier as a percentage string for <prosody rate=...>."""
    pct = round((rate - 1.0) * 100)
    return f"+{pct}%" if pct >= 0 else f"{pct}%"


def _volume_to_string(db: float) -> str:
    """Format a dB volume offset as a percentage for <prosody volume=...>."""
    # Azure volume is 0–100; default is 100. Map dB linearly (rough).
    pct = round(db * 1.0)
    return f"+{pct}%" if pct >= 0 else f"{pct}%"


def _apply_phrase_prosody(
    parent: ET.Element,
    text: str,
    phrases: list[PhraseProsody],
) -> None:
    """Inject per-phrase ``<prosody>`` and ``<break>`` elements into *parent*.

    Splits *text* around phrase spans and wraps each phrase in a nested
    ``<prosody>`` element with optional ``<break time="…"/>`` elements
    inserted before and/or after.  Overlapping spans are skipped (the span
    whose ``char_start`` falls before the previous span's ``char_end`` is
    silently dropped).

    Args:
        parent: The XML element that will receive the mixed text/element content.
        text: The utterance text (plain UTF-8).
        phrases: Resolved phrase spans with prosody directives.
    """
    cursor = 0
    prev: ET.Element | None = None

    def _append_text(s: str) -> None:
```

## Provenance

- Manifest: `state/manifests/sources/src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4.json`
- Workspace source: `synthbanshee/tts/ssml_builder.py`
- Run ID: `run-src-3d18f514d3e6bae4424eb2a3a8ff318976d5d5834a19fe6ddc16e9e8e6baebc4-20260430T200753740010Z`
- Pipeline version: `0.1.0a0`
