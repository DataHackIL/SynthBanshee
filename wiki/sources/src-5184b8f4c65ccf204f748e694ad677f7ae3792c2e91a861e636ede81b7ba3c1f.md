---
schema_version: '1'
kind: source-summary
title: preprocessing
page_id: src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f
status: active
review_state: machine-generated
source_refs:
- src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f
generated_by_run_ids:
- run-src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f-20260430T200821047909Z
last_generated_at: '2026-04-30T20:08:21+00:00'
last_reviewed_at: null
confidence: 1.0
related_pages: []
tags:
- source-summary
- py
provenance_links:
- source_id: src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f
  page_id: null
  run_id: null
  path_ref: state/manifests/sources/src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f.json
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: run-src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f-20260430T200821047909Z
  path_ref: null
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: null
  path_ref: synthbanshee/augment/preprocessing.py
  role: input
  note: null
contradictions: []
---

# preprocessing

## Source

- Source ID: `src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f`
- Source type: `py`
- Registered path: `synthbanshee/augment/preprocessing.py`
- Source file: `synthbanshee/augment/preprocessing.py`

## Summary

This page records deterministic ingestion output for source `src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f`, a `py` file registered from `synthbanshee/augment/preprocessing.py`.

## Key Facts

- Source ID: `src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f`
- Source type: `py`
- Checksum: `5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f`
- Source ref: `synthbanshee/augment/preprocessing.py`
- Added at: `2026-04-30T20:07:23+00:00`
- Ingested at: `2026-04-30T20:08:21+00:00`

## Extract

```text
"""Preprocessing pipeline: resample → downmix → low-pass → denoise → normalize → pad → validate.

Spec reference: docs/spec.md §3.1

Order of operations is fixed. The "dirty" pre-pipeline file is retained in assets/
and must never be overwritten by this module.

Uses scipy + soundfile exclusively (no torchaudio dependency at import time)
to avoid torch version incompatibilities.
"""

from __future__ import annotations

import math
import shutil
from dataclasses import dataclass, field
from pathlib import Path

import numpy as np
import soundfile as sf
from scipy.signal import butter, resample_poly, sosfilt, wiener

from synthbanshee.config.preprocessing_config import PreprocessingConfig

_TARGET_SR = 16_000
_TARGET_CHANNELS = 1
_PEAK_DBFS = -1.0  # −1.0 dBFS
_LP_CUTOFF_HZ = 7_500  # low-pass cutoff
_LP_ORDER = 4  # Butterworth order
_SILENCE_PAD_S = 0.5  # minimum silence padding (seconds)
_MIN_DURATION_S = 3.0  # clips below this are invalid (spec §3)
_MAX_DURATION_S = 300.0  # clips above this must be segmented (spec §3)

# Re-export so callers can do: from synthbanshee.augment.preprocessing import PreprocessingConfig
__all__ = ["PreprocessingConfig", "PreprocessingResult", "preprocess", "validate_audio"]


@dataclass
class PreprocessingResult:
    output_path: Path
    dirty_path: Path | None
    sample_rate: int
    channels: int
    duration_seconds: float
    peak_dbfs: float
    silence_pad_applied_s: float = _SILENCE_PAD_S
    steps_applied: list[str] = field(default_factory=list)
    warnings: list[str] = field(default_factory=list)


def _to_dbfs(samples: np.ndarray) -> float:
    """Return peak dBFS of a float32 array normalised to ±1.0."""
    peak = float(np.max(np.abs(samples)))
    if peak == 0.0:
        return -math.inf
    return 20.0 * math.log10(peak)


def _peak_limit(samples: np.ndarray, ceiling_dbfs: float = _PEAK_DBFS) -> np.ndarray:
    """Attenuate samples so peak ≤ ceiling_dbfs; never scale up.

    Unlike peak normalization, this only applies gain when the signal
    exceeds the ceiling.  Quieter signals are returned unchanged so that
    the within-scene loudness trajectory established by per-turn RMS gain
    (M3a) is preserved across the full clip.
    """
    peak = float(np.max(np.abs(samples)))
    if peak == 0.0:
        return samples
    ceiling_linear = 10.0 ** (ceiling_dbfs / 20.0)
    if peak <= ceiling_linear:
        return samples  # already within limit — do not scale up
    return (samples * (ceiling_linear / peak)).astype(np.float32)


def _resample(samples: np.ndarray, src_sr: int, dst_sr: int) -> np.ndarray:
    """Resample using polyphase filtering (scipy.signal.resample_poly)."""
    from math import gcd

    g = gcd(dst_sr, src_sr)
```

## Provenance

- Manifest: `state/manifests/sources/src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f.json`
- Workspace source: `synthbanshee/augment/preprocessing.py`
- Run ID: `run-src-5184b8f4c65ccf204f748e694ad677f7ae3792c2e91a861e636ede81b7ba3c1f-20260430T200821047909Z`
- Pipeline version: `0.1.0a0`
