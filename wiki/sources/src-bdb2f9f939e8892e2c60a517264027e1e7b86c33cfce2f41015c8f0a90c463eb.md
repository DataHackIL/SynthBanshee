---
schema_version: '1'
kind: source-summary
title: speaker state
page_id: src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb
status: active
review_state: machine-generated
source_refs:
- src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb
generated_by_run_ids:
- run-src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb-20260430T200839352940Z
last_generated_at: '2026-04-30T20:08:39+00:00'
last_reviewed_at: null
confidence: 1.0
related_pages: []
tags:
- source-summary
- py
provenance_links:
- source_id: src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb
  page_id: null
  run_id: null
  path_ref: state/manifests/sources/src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb.json
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: run-src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb-20260430T200839352940Z
  path_ref: null
  role: generated-from
  note: null
- source_id: null
  page_id: null
  run_id: null
  path_ref: synthbanshee/tts/speaker_state.py
  role: input
  note: null
contradictions: []
---

# speaker state

## Source

- Source ID: `src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb`
- Source type: `py`
- Registered path: `synthbanshee/tts/speaker_state.py`
- Source file: `synthbanshee/tts/speaker_state.py`

## Summary

This page records deterministic ingestion output for source `src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb`, a `py` file registered from `synthbanshee/tts/speaker_state.py`.

## Key Facts

- Source ID: `src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb`
- Source type: `py`
- Checksum: `bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb`
- Source ref: `synthbanshee/tts/speaker_state.py`
- Added at: `2026-04-30T20:07:23+00:00`
- Ingested at: `2026-04-30T20:08:39+00:00`

## Extract

```text
"""Stateful cross-turn emotional controller (M7 — spec §4.3).

One ``SpeakerState`` instance is created per speaker at the start of
``TTSRenderer.render_scene()`` and updated after every turn.  The accumulated
prosody offsets are applied on top of the base ``StyleEntry`` values so that a
speaker who has been escalating for several turns carries that momentum into
subsequent turns rather than resetting to a neutral baseline at each turn
boundary.

Design doc reference: docs/audio_generation_v3_design.md §4.3
"""

from __future__ import annotations

from dataclasses import dataclass, field

# ---------------------------------------------------------------------------
# Per-role intensity target tables
#
# Each entry maps an intensity level (1–5) to the target state offsets that
# the speaker converges toward as turns at that intensity accumulate:
#   (rate_offset_multiplier, pitch_offset_semitones, volume_offset_db)
#
# AGG: higher intensity → faster rate, higher pitch, louder.
# VIC: higher AGG pressure → VIC compliance shifts to slower, lower volume,
#      slight pitch rise from tension.
# ---------------------------------------------------------------------------

_AGG_TARGETS: dict[int, tuple[float, float, float]] = {
    1: (1.00, 0.0, 0.0),
    2: (1.03, 0.3, 1.0),
    3: (1.08, 0.8, 2.0),
    4: (1.12, 1.5, 3.0),
    5: (1.15, 2.0, 4.0),
}

_VIC_TARGETS: dict[int, tuple[float, float, float]] = {
    1: (1.00, 0.0, 0.0),
    2: (0.98, 0.2, -0.5),
    3: (0.94, 0.5, -1.5),
    4: (0.90, 0.8, -2.5),
    5: (0.87, 1.0, -3.0),
}

# Unknown roles (bystanders, witnesses, etc.) hold neutral state.
_NEUTRAL_TARGETS: dict[int, tuple[float, float, float]] = {k: (1.0, 0.0, 0.0) for k in range(1, 6)}

# Fraction of the gap between current state and target that is closed per turn
# when intensity is steady or rising (escalation).
_DRIFT_RATE_ESCALATE: float = 0.60

# Fraction closed per turn when intensity drops (decay).
_DRIFT_RATE_DECAY: float = 0.30


def _target_for(role: str, intensity: int) -> tuple[float, float, float]:
    """Return the target offsets for *role* at *intensity* (clamped to 1–5)."""
    table: dict[int, tuple[float, float, float]]
    if role == "AGG":
        table = _AGG_TARGETS
    elif role == "VIC":
        table = _VIC_TARGETS
    else:
        table = _NEUTRAL_TARGETS
    return table[max(1, min(5, intensity))]


@dataclass
class SpeakerState:
    """Accumulated prosody-offset state for a single speaker across turns.

    Starts at neutral (rate_offset=1.0, all deltas 0.0).  Call
    ``update(turn.intensity, speaker.role)`` after each rendered turn to drift
    the state.  Pass the state to ``TTSRenderer.render_utterance()`` via the
    ``speaker_state`` kwarg so the offsets are baked into the SSML.

    Attributes:
        intensity_history: Ordered list of intensities seen for this speaker.
        rate_offset: Multiplicative rate modifier applied on top of
            ``StyleEntry.rate_multiplier``.  Starts at 1.0 (no change).
```

## Provenance

- Manifest: `state/manifests/sources/src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb.json`
- Workspace source: `synthbanshee/tts/speaker_state.py`
- Run ID: `run-src-bdb2f9f939e8892e2c60a517264027e1e7b86c33cfce2f41015c8f0a90c463eb-20260430T200839352940Z`
- Pipeline version: `0.1.0a0`
