# SynthBanshee — Agent State Tracker

**Last updated:** 2026-04-16
**Branch convention:** `feat/<milestone-id>-<slug>`, `fix/<slug>`, `docs/<slug>`, `refactor/<slug>`

---

## Current system state

All Phase 0 and Phase 1 milestones are complete and merged to main.

**V3 milestones completed (on main):**
- **M1** — Hebrew gender disambiguation (niqqud + SSML phoneme injection)
- **M2a** — Speaker pitch corrections (VIC F0 lowered, AGG pitch capped)
- **M3a** — Per-turn RMS gain in SceneMixer (4-tuple segment API, `_apply_rms_gain()`)
- **M3b** — Peak limiter in `preprocess()` (replaces normalizer; quiet clips preserved)
- **IAA utilities** — per-category Cohen's kappa, `IAAReport`, `iaa-report` CLI

**Key architectural invariants now in place:**
- `SceneMixer.mix_sequential()` takes `(wav_bytes, pause_s, speaker_id, rms_target_dbfs)` 4-tuples
- Temp WAV between mixer and `preprocess()` is written as `subtype="FLOAT"`
- `preprocess()` uses `_peak_limit()` (limiter), not `_normalize_peak()` (normalizer)
- `validate_audio()` rejects peak > −1.0 dBFS + 0.5 dB tolerance; quiet clips pass

---

## Active / next task: M4 — Emotional-State Metadata Consistency

**Priority:** P0
**Branch to create:** `feat/m4-emotion-consistency`
**Goal:** Fix silent label corruption where `_normalize_emotion()` silently maps unknown emotional states to a fallback, corrupting the strong-label JSONL.

### Checklist

- [ ] **Audit** `debug_run_1` output: collect all unique emotional state strings that appear in generated JSONL
- [ ] **Audit** `configs/taxonomy.yaml` `emotional_states` section: list all currently accepted states
- [ ] **Gap analysis**: identify LLM output states not covered by taxonomy
- [ ] **Extend `taxonomy.yaml`** `emotional_states` to cover all legitimate LLM outputs identified above
- [ ] **Replace `_normalize_emotion()` silent fallback** with an explicit mapping table + `logging.warning()` for downgrade cases; hard fail (`ValueError` or similar) for completely unknown states
- [ ] **Add `WARN_EMOTION_DOWNGRADE` QA flag** in `synthbanshee/package/qa.py`
- [ ] **Unit tests**: verify warning is emitted on downgrade; verify hard fail on unknown state
- [ ] **Update `docs/audio_generation_v3_design.md`** — mark M4 as ✅ Done in tracker

### Key files to touch
- `configs/taxonomy.yaml` — extend `emotional_states` list
- `synthbanshee/labels/generator.py` — find and replace `_normalize_emotion()`
- `synthbanshee/package/qa.py` — add `WARN_EMOTION_DOWNGRADE` flag
- `tests/unit/test_labels.py` — add emotion normalization tests

---

## Upcoming milestones (P1 queue)

| ID | Slug | Key change |
|----|------|-----------|
| M5 | preprocessing-config | `PreprocessingConfig` dataclass; Wiener denoising becomes a configurable flag |
| M6 | turn-gap-controller | `TurnGapController` replacing fixed `pause_before_s` |
| M7 | speaker-state | `SpeakerState` with intensity-drift rules across turn boundaries |
| M2b | phrase-prosody | `PhraseHint`/`PhraseProsody` + char-offset resolver for nested SSML |
| M8a | overlap-mixing | `MixMode` enum; `SceneMixer` overlap/barge-in assembly |
| M8b | overlap-labels | Three-concept onset schema; `audible_onset_s` used by label generator |

Full detail: `docs/audio_generation_v3_design.md` §Implementation Tracker

---

## Context pointers

| What | Where |
|------|-------|
| Full milestone tracker | `docs/audio_generation_v3_design.md` → Implementation Tracker |
| Schema spec | `docs/spec.md` |
| Risk register / cost estimates | `docs/implementation_plan.md` |
| Architecture index | `llms.txt` |
| Hard rules for all agents | `AGENTS.md` (CLAUDE.md symlinks here) |
| Taxonomy labels | `configs/taxonomy.yaml` |
| Example configs | `configs/examples/` |
